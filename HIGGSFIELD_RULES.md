# HIGGSFIELD_RULES — Seed 비주얼 에셋 제작 규칙

> Higgsfield 관련 작업을 시작하기 전에 이 파일을 먼저 읽는다. 이 규칙이 Higgsfield 스킬의 기본 동작보다 우선한다.
> 작성 2026-09-21 · 기준 커밋 9451b01

## 1. 목적과 역할

목표는 **게임을 새로 만드는 것이 아니라**, 적은 수의 고품질 원본을 잘 합성하고 재사용해서 모바일 성능을 지키며 한 단계 완성된 화면을 만드는 것이다.

| 담당 | 하는 일 |
|---|---|
| Claude Code | 기존 코드 관리, 게임 로직, UI/UX, Three.js 효과·셰이더, 에셋 가공·적용, Firebase, 성능 검사 |
| Higgsfield | **원본 이미지만**: 캐릭터·몬스터·보스 컨셉, 배경, 텍스처, 탄환 소재, UI 장식 소재, VFX 텍스처 소재 |
| GPT(기존 그림 담당) | 카드 원화 아틀라스 등 지금 맡은 그림 작업은 그대로. Higgsfield 결과물도 같은 아트 디렉션을 따른다 |

Higgsfield는 게임 엔진·게임 코드·웹사이트를 만드는 데 쓰지 않는다. 설치한 스킬도 이미지 생성(`higgsfield-generate`) 하나뿐이다(웹사이트·게임 생성 스킬은 설치하지 않음).

## 2. 절대 금지

1. 기존 Seed 게임 전체를 새로 생성하지 않는다.
2. 기존 게임 시스템을 Higgsfield 결과물에 맞춰 뜯어고치지 않는다.
3. 조작감·밸런스·Firebase 구조를 임의로 바꾸지 않는다.
4. 4K 이미지를 게임에 그대로 넣지 않는다. 원본은 `public/`에 두지 않는다.
5. 플레이 중 MP4·영상 반복 재생 방식을 쓰지 않는다. 영상 생성 자체를 하지 않는다.
6. GIF를 이펙트로 쓰지 않는다.
7. 조합(최대 990개)마다 고해상도 이미지를 하나씩 만들지 않는다.
8. 같은 형태·같은 프롬프트를 반복 생성하지 않는다.
9. 크레딧을 대량으로 자동 사용하지 않는다.
10. 사용자가 요청하지 않은 대규모 에셋 생성을 하지 않는다.

## 3. 크레딧 규칙

- 한 작업에서 **최대 3개**까지 생성한다(사용자가 더 요청하면 그때만).
- 영상·고해상도 대량 생성·variation 대량 생성·같은 프롬프트 반복은 하지 않는다.
- 기존 에셋을 재활용할 수 있으면 새로 만들지 않는다.
- 생성 전에 **반드시** 아래 형식으로 보고하고 사용자 승인을 받는다. 가능하면 `higgsfield generate cost …`로 크레딧 비용을 먼저 확인해 함께 적는다(스킬 기본값은 비용 확인을 생략하지만 이 규칙이 우선한다).

```
이번 작업에 필요한 Higgsfield 생성:
1. 1막 바닥 텍스처 1개 (모델 ○○ · 예상 ○크레딧)
2. …
총 N개 · 예상 합계 ○크레딧
```

- 해상도는 필요한 만큼만 요청한다. 게임에 들어갈 최종 크기의 2배를 넘는 원본은 요청하지 않는다(예: 256px 소재는 512~1K 원본이면 충분).

## 4. Seed 아트 디렉션

- **분위기**: HD 2D 회화 질감의 어두운 식물 판타지. 잠든 정원, 이끼 낀 돌, 뿌리, 등불, 옥빛 빛.
- **기본 팔레트**: 상아(#e9eedc 계열) · 옥빛(#76ffd0) · 절제된 금색(#ffc84f 계열) · 깊은 청록 바탕(#10252a).
- **법칙 색**(알아보는 데만 씀): 반사 #73dfff · 분열 #ff947b · 연쇄 #ffdc73 · 공전 #9ca8ff · 관통 #e1ffad · 폭발 #ffad58 · 귀환 #8dffcd · 중력 #da9aff · 빙결 #9af0ff.
- **시점**: 위에서 약 55° 내려다보는 3/4 시점. 캐릭터·몬스터는 판자(빌보드) 그림이라 정면에 가까운 3/4 포즈, 발밑이 바닥에 닿는 구도.
- **빛**: 왼쪽 위 주광 + 차가운 테두리 빛(게임의 명암 셰이더가 이 방향을 가정한다). 그림 안에 강한 광원·그림자를 따로 그리지 않는다.
- **읽힘**: 20~48px로 줄여도 형태(핵·외피·탄두·꼬리 방향)가 읽혀야 한다.
- **피할 것**: 일반 애니풍, 과도한 네온, 단순 원형 폭발, 네모 아이콘, 사진풍 실사, 글자·워터마크.
- 게임 출력에서 채도 +20%가 더해지므로 원본은 과포화하지 않는다.

## 5. 해상도·포맷·파일명

| 용도 | 게임 적용 크기 | 비고 |
|---|---|---|
| 작은 탄환·VFX 소재 | 64~256px (칸) | 512×512 아틀라스에 모음 |
| 작은 몬스터 | 256~512px | 휴대폰본 512 이하 |
| 큰 몬스터·보스 | 512~1024px | 휴대폰본 512 |
| 바닥·벽 타일 텍스처 | PC 1024 · 휴대폰 512 | 이음새 없는(타일링) 그림 |
| 배경 원경 | 화면에 보이는 만큼만 | 가로 2048 미만 |
| UI 장식 | 실제 표시 크기의 2배까지 | |

- 2048px 이상은 특별한 이유 없이 런타임 에셋으로 쓰지 않는다. **4096px 금지.**
- 기본 포맷은 **WebP**(품질 80~90). 투명이 필요하면 WebP 알파. PNG는 무손실이 꼭 필요한 경우만.
- 가산 합성(빛) VFX는 **순검정 바탕**으로 만들어 알파 없이 써도 된다(검정 = 안 보임).
- WebGL 텍스처는 가능하면 2의 거듭제곱(256·512·1024)으로 맞춰 밉맵을 쓴다.
- 파일명: `<분류>-<이름>-v<번호>[-ui].webp` (영문 소문자·하이픈, 한글·공백 금지). 예: `vfx-atlas-v1.webp`, `ground-garden-v5.webp`.
- 휴대폰본은 `public/assets/mobile/`에 같은 이름으로(적 그림은 이미 이 방식).
- Higgsfield 원본은 `art-source/higgsfield/<날짜>/`에 보관하고 git에 올리지 않는다(`.gitignore`). `public/`에는 가공한 결과만 넣는다.

## 6. 아틀라스 규칙

- 같은 순간에 함께 쓰는 소재는 한 장으로 묶는다(텍스처 바인딩·로드 수 절약).
- VFX·탄환 소재: 512×512, 128px 칸 4×4(칸 사이 2px 여백). 한 장이 16개 소재.
- UI 카드 아틀라스는 기존 방식 유지: 1024×768, 256px 칸 4×3.
- 적 그림: 기존 방식 유지(방향 4칸, PC 768 · 휴대폰 512).

## 7. 탄환·조합 표현 규칙 (990개 대응)

조합마다 완성 그림을 만들지 않는다. **공통 소재의 조합**으로 만든다.

```
공통 기본 탄환(메시) + 소재 텍스처 + 무늬 + 빛(글로우) + 꼬리 + 입자 + 셰이더 + 적중 효과
+ 색·크기·속도·회전 등 매개변수
```

- 예: 폭발+반사 = 불꽃 바탕 소재 + 반사 고리 + 주황 빛 + 불꽃 입자 + 반사 꼬리. 새 완성 이미지는 없다.
- 소재는 기존 '탄환 유전자'(중심핵·외피·탄두·꼬리·표식) 구조와 맞춘다. 법칙마다 핵·외피·꼬리 소재 몇 장이면 충분하다.
- 전용 원화는 선택률·상징성이 높은 조합만(지금은 GPT 카드 아틀라스가 담당).

## 8. VFX·배경·궁극기

- **VFX**: 기존 인스턴스 묶음(파티클·탄환 묶음)에 텍스처만 입힌다. 효과 하나가 드로콜 하나를 새로 만들지 않는다. 투명 겹침(오버드로)이 큰 큰 판은 크기·수명을 먼저 계산한다.
- **배경**: 영상 대신 `정적 고품질 이미지 + 시차(parallax) + 빛 레이어 + 입자 + 안개 + 셰이더 + 카메라 움직임`.
- **궁극기**: 영상 없이 `작은 VFX 텍스처 + 섬광 + 흔들림 + 방사형 효과 + 입자 + 셰이더 + 크기 애니메이션 + 히트스톱 + 카메라`. 궁극기 일곱 골격(BURST·RAIN·ORBIT·BEAM·DOMAIN·BLACKHOLE·TIME STOP)마다 소재 1칸씩이면 된다.

## 9. 성능 규칙 (iPhone 13급 기준)

- 적용 절차: `Higgsfield 원본 → 필요한 영역 크롭 → 리사이즈 → 포맷 선택 → 표시 크기에 맞게 축소 → (필요하면) 아틀라스 → 게임 적용 → 성능 측정 → 기존 버전과 비교 → 사용자 승인`.
- 매번 확인: FPS·프레임 시간(P95/P99)·드로콜·텍스처 메모리·파티클 수·이미지 로드 크기·DOM 수. 도구: 게임 안 조용한 성능 감시(버전 번호 7번 탭 → DEV 메뉴), 배포용 빌드 비교(`vite build` + `vite preview`, 주소 `?inspect=1&adminLab=1`).
- 새 재질 종류를 만들면 `src/main.js`의 `buildShaderWarmGroup()`에 견본을 넣고, `seedDebug.qa.programs()`로 판 시작·전투 중 새 셰이더 컴파일이 0개인지 확인한다.
- 새 탄 종류는 `shotObject`/`addShotParts`(인스턴스 묶음)로 만들고, 반투명 부분은 묶지 않는다.
- 텍스처 메모리 계산: 1024² RGBA ≈ 4MB(밉맵 포함 약 5.3MB), 512² ≈ 1.3MB. 한 화면에 새로 올리는 텍스처 합계는 PC 16MB · 휴대폰 8MB 안쪽으로.
- 품질을 올린 뒤 성능이 떨어지면 **효과를 먼저 지우지 않는다**: 연출 유지 → 최적화 → 그래도 안 되면 단계적 축소. 자동으로 화질을 낮추는 기능은 넣지 않는다(사용자 결정).

## 10. 현재 연결 상태 (2026-09-21)

연결 통로가 두 개다. 어느 쪽으로 생성하든 위 규칙(작업당 3개·생성 전 보고·영상 금지·4K 금지)은 똑같이 적용한다.

- **공식 CLI** `@higgsfield/cli` 1.1.26 설치(공식 저장소 higgsfield-ai/cli). 공식 스킬 `higgsfield-generate`만 설치(`~/.claude/skills/`).
  - 로그인 전: 사용자가 직접 `higgsfield auth login`(브라우저 로그인, API 키 불필요) 후 `higgsfield workspace list` → `higgsfield workspace set <id>`로 결제 작업 공간을 고른다.
  - 생성 없이 쓸 수 있는 확인 명령: `higgsfield account status`(플랜·크레딧), `higgsfield model list`, `higgsfield generate cost …`(비용 미리 보기). `higgsfield auth token`은 비밀 값을 출력하므로 쓰지 않는다.
- **공식 MCP 서버** `https://mcp.higgsfield.ai/mcp`(Higgsfield 공식 도움말에 안내된 주소). Claude Code 사용자 설정에 `higgsfield`라는 이름으로 등록했다(HTTP, 모든 프로젝트 공통).
  - 인증 전: 사용자가 대화형 `claude` 터미널에서 `/mcp` → higgsfield → 인증(브라우저 OAuth)을 직접 한다. 토큰·코드를 Claude에게 붙여 넣지 않는다.
  - MCP에는 4K 이미지·영상 생성 도구도 들어 있다. **쓰지 않는다.** 이미지 생성만, 필요한 해상도만.
  - 비용 미리 보기는 CLI `higgsfield generate cost`로 먼저 확인한다(MCP 도구로 비용을 알 수 없을 때).
- **크레딧 주의**: 에이전트(CLI·MCP)로 생성하면 플랜과 상관없이 **매번 크레딧이 차감된다.** 웹사이트의 무제한·무료 생성 혜택은 적용되지 않는다(Higgsfield 공식 도움말).
- 2026-09-21 사용자가 MCP 인증 완료. 계정: 무료 플랜, 작업 공간은 개인 하나. 무료 플랜은 GPT Image 2.5가 막혀 있다("basic 플랜 이상 필요", 시작 전에 거절돼 크레딧은 빠지지 않음). Nano Banana 2(1K 1.5크레딧)는 된다.
- 비용 미리 보기: MCP `generate_image`에 `get_cost:true`를 주면 생성하지 않고 크레딧만 알려 준다. 조회한 값(1K 기준): Z Image 0.15 · FLUX.2 pro 1 · GPT Image 2.5 medium 1 / high 2 · Recraft V4.1 1.25 · Nano Banana 2 1.5 · GPT Image 2 high 3.5.

## 11. 생성 기록 (같은 것을 다시 만들지 않기 위해)

| 날짜 | 무엇 | 모델 · 크레딧 | 원본 | 게임 적용 |
|---|---|---|---|---|
| 2026-09-21 | 전투 이펙트 소재 시트(4×4, 흑백, 검정 바탕): 빛 구슬·별빛·빛 조각·빛 고리·불꽃 혀·빛 꼬리·혜성·초승달 베기·잎·꽃잎·얼음 조각·번개·안개·홀씨·충격 고리·덩굴 문양 | Nano Banana 2 1K · 1.5 | `art-source/higgsfield/20260921/vfx-sheet-nb2-1k.png`(1024², 1.1MB) | `tools/vfx-atlas-build.py` → `public/assets/vfx-atlas-v1.webp`(512², 18KB). 불꽃 조각·불꽃 혀 묶음(`src/vfx.js`)에 입힘. 칸 번호는 `VFX_CELLS` |
| 2026-09-22 | 탄환 소재 시트(4×4, 흑백, 검정 바탕): 법칙 빛 무늬 10종(씨앗 잎·프리즘·꽃잎 부채·전기 구슬·고리 행성·창끝·불꽃 고리·초승달 고리·소용돌이·눈꽃) + 치명타 별 + 꼬리 5종(부드러운 빛·반짝이·잎·번개·서리 안개) | Nano Banana 2 1K · 1.5 | `art-source/higgsfield/20260922/shot-sheet-nb2-1k.png` | `public/assets/shot-atlas-v1.webp`(512², 23KB). 씨앗 탄 뒤에 빛 무늬 + 꼬리 두 장(`src/shot-auras.js`, 한 묶음·드로콜 +1). 첫 법칙 = 무늬, 둘째 법칙 = 꼬리. 칸 번호는 `SHOT_CELLS` |
