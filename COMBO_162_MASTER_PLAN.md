# SEED 조합 구조 · 162개 기준

2026-09-23 사용자 결정. 이 문서는 이전 `COMBO_1090_MASTER_PLAN.md`의 **플레이어가 실제로 만드는 조합 구조**를 대체한다. 기존 990개 재융합 데이터와 발견 기록은 삭제하지 않지만 새 도감에서는 숨긴다.

| 분류 | 수 | 재료 | 현재 상태 |
| --- | ---: | --- | --- |
| 기본 성질 | 9 | 선택 가능한 기본 법칙 | 플레이 가능 |
| 단독 진화 | 9 | 기본 성질 한 가지 Lv.5 | 플레이 가능 |
| 1차 융합 | 36 | 서로 다른 기본 성질 두 가지 | 전부 손설계, 베타 선택 가능 |
| 완성 각성 | 72 | 1차 융합(A+B) + A 또는 B의 **단독 진화 한 가지** | 11개 기존 갈래·61개 새 갈래, 베타 선택 가능 |
| 쌍둥이 각성 | 36 | 서로 다른 단독 진화 두 가지 | 36개 레시피·공명 규칙 플레이 가능 |
| **합계** | **162** | | |

`docs/COMBO_162_FINAL_BRANCH_MATRIX.json`이 36쌍·72갈래의 코드 ID, 원본 초안 ID, 추가 단독진화, 역할, 약점, 시각 표식, 구현 상태를 담는다. 기존 11개와 새 61개를 베타 선택지·도감에 연결했다. 원본 제안 72개는 `docs/SEED_FINAL_EVOLUTIONS_72_DRAFT_v1.json`에 보존했고 `node tools/build-final-evolution-matrix.mjs`로 다시 대조할 수 있다.

## 서로 다른 갈래의 기준

같은 융합의 A분기와 B분기는 이름·색·숫자만 달라서는 안 된다. 전투 중 **공격 중심 위치, 이동 경로, 타격 순서, 지속 시간 중 하나 이상**이 달라야 한다. 추가한 단독진화의 성질이 주도권을 가져야 하고, 원래 융합의 두 성질도 읽혀야 한다. 같은 한 판에서 같은 레벨·같은 적 배치로 두 분기의 적중 횟수, 단일 대상/무리 피해, 보스전 시간, 화면 가독성, 낮은 사양 프레임을 비교한다. 한 분기가 다른 분기의 완전한 상위 호환이면 공개하지 않는다.

첫 검증쌍은 폭발+중력이다. **대붕괴 = 폭발 단독을 더한 갈래**로 큰 한 번의 파열을 유지한다. **맥동중력핵 = 중력 단독을 더한 갈래**로 같은 위치에 오래 남아 세 번 맥동하고 약하게 닫힌다. 보스 이동 면역은 그대로 적용한다. 이 비교를 기준으로 다음 묶음을 만든다. 초안의 72개 가칭을 자동으로 게임에 넣지 않는다.

## 쌍둥이와 옛 데이터

두 단독 진화의 36쌍은 언제나 쌍둥이다. 예전에는 10쌍이 융합 각성에 잘못 합쳐졌고 26쌍만 별도 쌍둥이였지만, 이제 10쌍도 별도 ID다. 두 단독 공격과 여는 기술을 유지하면서 36쌍 각각의 교차 적중 규칙을 적용한다. 카드 그림은 두 단독 원화의 합성 아틀라스이므로 독립 원화로서의 품질은 계속 검수한다.

990개 자동 재융합 목록 및 손설계 재융합 20개는 기록 호환을 위해 코드에 보존한다. 새 162개 구조에는 세지 않고 플레이어 도감에서 숨긴다. 기존 저장을 열었을 때 알 수 없는 진화가 되어서는 안 된다. 레거시 기록을 새 칭호·발견률로 잘못 더하지 않는지도 검수한다.

## 공개 전 검수

1. 61개 새 갈래의 실제 역할과 약점이 설명과 일치하는지 같은 전장에서 대응되는 두 갈래를 비교한다.
2. 36개 쌍둥이 공명이 보스에게 무한 연계되지 않는지, 궁극기 충전과 겹침이 과하지 않은지 실기에서 확인한다.
3. 새 합성 원화 가운데 멀리서 비슷하게 보이는 카드는 따로 다듬고, 모바일에서 탄/적탄과 공격 방향을 구분한다.
4. 이번 베타는 6개 추가 융합과 61개 갈래를 자동 검사·데스크톱 모바일 크기 시연 뒤 열었다. 실제 저사양 Android의 긴 전투 프레임·발열 및 학생의 보스/무리 체감은 베타 운영 중 계속 검수하고 문제가 있으면 개별 갈래를 조정한다.

`tools/evolution-taxonomy-test.mjs`가 9+9+36+72+36의 레시피 중복·ID 연결·보류 상태를 확인하고, `tools/awaken-test.mjs`가 실제 합성 경로를 확인한다.
