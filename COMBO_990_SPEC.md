# SEED 조합 990 구현 기준

## 확정 구조

- 기본 법칙 10개: 반사, 분열, 연쇄, 공전, 관통, 폭발, 귀환, 중력, 빙결, 차원
- 서로 다른 기본 법칙 2개를 합친 1차 융합: `10C2 = 45`
- 서로 다른 1차 융합 2개를 다시 합친 재융합: `45C2 = 990`
- 같은 1차 융합을 자기 자신과 합치는 경우는 제외한다. 그래서 1,035개가 아니라 990개다.

## 재융합 두 계열

1. 공명형 360개: 두 1차 융합이 기본 법칙 하나를 공유한다. 공유 법칙의 성질이 세 번째 적중마다 증폭되고 두 후속 효과가 번갈아 발동한다.
2. 교차형 630개: 네 기본 법칙이 모두 다르다. 첫 1차 융합이 표식을 남기고 둘째 1차 융합이 표식을 소비해 교차 효과를 낸다.

네 법칙의 투사체 수, 피해, 범위 보너스를 전부 곱하지 않는다. 재융합마다 주 공격 하나와 후속 효과 하나만 고르고, 나머지는 조건과 외형에 반영한다. 이 제한이 대붕괴 같은 과도한 조합을 막는 기본 규칙이다.

## 현재 들어간 파일

- `src/combo-catalog.js`: 10개, 45개, 990개의 고정 ID, 이름, 계열, 규칙, 밸런스 예산, 이미지 유전자
- `src/combo-art.js`: 선택 카드와 투사체용 아틀라스 조합기
- `src/combo-art.css`: 중심핵과 외피를 겹치는 가벼운 표시 방식
- `public/assets/seed-law-atlas-v4-ui.webp`: 기존 아홉 법칙과 새 차원 법칙
- `public/assets/seed-law-portal-v1.webp`: 차원 법칙 투명 원화
- `public/assets/combo-cores-v1.webp`: 중심핵 12종
- `public/assets/combo-shells-v1.webp`: 외피와 후광 12종
- `public/assets/combo-projectiles-v1.webp`: 탄환 12종

ImageGen 원본은 `C:/Users/이소아빠/.codex/generated_images/01a07599-6b5d-76c2-a8bb-acbacefb7ab9/`에 보존한다.

- `exec-9f0c2c1b-98e3-4432-8088-b9f72f5ee71d.png`: 기존 법칙 아틀라스를 화풍 기준으로 삼은 차원 씨앗 문양
- `exec-0852af0e-c6db-4c1e-b54c-f9d1154dda5f.png`: 비어 있는 중앙에 핵을 겹칠 수 있는 외피와 후광 12종
- `exec-9217f592-ddfd-4a2e-947b-acf54ccd3822.png`: 열 법칙, 뿌리, 각성을 나타내는 중심핵 12종
- `exec-496238fc-3c32-429f-8149-59deb6350b03.png`: 작은 화면에서도 원이 아닌 실루엣으로 읽히는 탄환 12종

공통 프롬프트 기준은 투명 배경, 글자·그림자 없음, 어두운 식물 판타지, 상아·옥·절제된 금색, 20~48px에서 읽히는 굵은 실루엣, 애니메이션풍 금지다.

## 클로드 구현 순서

1. `portal`을 실제 열 번째 법칙으로 넣고 도약 공격을 구현한다. 발사체가 입구에서 사라져 목표 가까운 출구에서 다시 나타나게 하되, 화면 밖 순간 피격은 금지한다.
2. 기존 10개 수동 `FORMS`는 같은 조합의 특별 원화와 공격을 계속 사용한다. 나머지 35개는 `FIRST_FUSIONS`의 규칙으로 생성하고, 이후 인기 조합부터 전용 공격으로 승격한다.
3. 재융합 선택 조건은 서로 다른 1차 융합 두 개를 보유했을 때다. 재융합은 두 슬롯을 한 슬롯으로 합치며, `SECOND_FUSIONS`의 `budget`과 `rule`을 사용한다.
4. 선택 카드는 `comboArt()`로 중심핵과 외피를 겹친다. 인게임 탄환은 `comboProjectileArt()`의 타일을 쓰되 한 화면에서 새 스프라이트 객체를 계속 만들지 말고 풀링한다.
5. 정원에는 재융합의 고정 ID를 저장하고 `visual.coreTile`, `shellTile`, `mark`는 카탈로그에서 다시 계산한다. 990장 이미지를 저장하지 않는다.
6. 도감은 한 번에 990개 DOM을 만들지 말고, 보이는 행만 렌더링한다. 모바일 저사양 기준으로 선택 화면에는 최대 3개 조합만 표시한다.

## 2026-09-17 구현 상태

- 1번 완료: `portal` 법칙, 안전 도약, 전용 투사체/VFX, 단독 진화와 정원 씨앗이 실제 게임에 연결됨.
- 2번 완료: 기존 10종 전용 공격을 보존했고 나머지 35종이 선택·저장·전투·강화·카드 그림·시그니처까지 작동함.
- 궁극기 완료: 신규 조합은 2방향 개문 공격과 강화 상태를 쓰며, 차원 태그는 타원형 문 무대와 먼 적 3명 피날레를 가짐. 기대 화력 12~32초분 검증.
- 성능: 신규 탄은 한 메시, 차원문은 기존 4개 VFX 배치, 궁극기는 기존 5드로우콜을 유지. 신규 35종 본체는 두 법칙 광륜을 공유해 몸 텍스처를 35장 늘리지 않음.
- 3번 완료: 보유한 서로 다른 1차 융합 두 개만 검사해 최대 세 장을 제시하고, 선택하면 두 슬롯을 한 슬롯으로 압축한다. 360개 공명형은 세 번째 적중 후속타, 630개 교차형은 2.6초 표식-소비 공격이 실제 전투에서 작동한다.
- 4번 완료: 선택 카드·도감·궁극기 버튼은 공유 핵/외피 아틀라스를 조합하고, 전투는 한 메시 gene-projectile과 기존 VFX 풀을 사용한다.
- 5번 완료: 정원은 최근 12개 빌드의 재융합 고정 ID, 레벨, 지배 법칙, 처치와 보스 기록을 저장한다. 그림 유전자는 ID로 다시 얻는다.
- 6번 완료: 선택 화면은 최대 세 장, 게임 도감은 최근 발견 24장, 전체 조합 연구소는 페이지당 24장만 렌더링한다.
- 궁극기 골격: BURST, RAIN, ORBIT, BEAM, DOMAIN, BLACKHOLE, TIME STOP 일곱 형태가 같은 5 draw-call 무대를 서로 다른 비율·회전·입자 흐름으로 사용한다.
- 다음 작업: 실제 저사양 안드로이드에서 밀집 보스전을 측정하고, VOID/CYBER/CELESTIAL 테마 프로토타입을 대표 조합에만 적용한다.

## 저장 호환

- 저장에는 배열 순번 대신 `f01-reflect-split`, `x001-01-02` 같은 고정 ID를 기록한다.
- 이전 저장에는 새 필드를 기본값으로 채우고, 현재 9법칙 여정과 기존 진화는 그대로 불러온다.
- 2막과 PVP는 이 구현과 분리한다.
